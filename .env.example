# Sample environment variables for the `join-waitlist` edge function.
# Add these in Lovable Cloud → Edge Function Secrets (not committed to code).

# MongoDB
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_DB_NAME=qikmove

# Brevo SMTP (https://app.brevo.com/settings/keys/smtp)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=adf665001@smtp-brevo.com
SMTP_PASSWORD=<your-brevo-smtp-master-password>
SMTP_FROM_EMAIL=hi.qikmove@11400805.brevosend.com
SMTP_FROM_NAME=QikMove